# sed

+ 修改文本

+ sed是一个流编辑器，用于对文件或文本进行替换，删除，添加或选取特定内容。

## 1.基本语法

sed [选项] '操作命令' 文件

sed 是一个流编辑器，用于对文件或文本进行替换、删除、添加或选取特定内容。

## 2.常用选项

| 选项    | 功能                               |
| ------- | ---------------------------------- |
| -e      | 允许多条 sed 命令                  |
| -i      | 直接编辑文件（修改原文件）         |
| -n      | 取消默认输出，只显示符合条件的内容 |
| -f 文件 | 从指定文件中读取 sed 命令          |
| --help  | 显示帮助信息                       |

## 3.基本用法

#### 3.1 替换文本

```shell
sed 's/旧字符串/新字符串/' file.txt
s：表示替换。
只替换每行的第一个匹配。
```

#### 3.2 替换所有匹配的字符串

```shell
sed 's/旧字符串/新字符串/g' file.txt
g：表示全局替换。
```

#### 3.3 替换第 N 次匹配

```shell
sed 's/旧字符串/新字符串/3' file.txt
只替换每行中第 3 个匹配项。
```

#### 3.4 在原文件中直接替换

```shell
sed -i 's/旧字符串/新字符串/g' file.txt
-i：直接修改原文件。
```

## 4.删除行

### 4.1 删除第 N 行

```shell
sed 'Nd' file.txt
示例：删除第 3 行：
sed '3d' file.txt
```

### 4.2 删除特定范围的行



```shell
sed 'M,Nd' file.txt
示例：删除第 2 到第 4 行：

sed '2,4d' file.txt
```

### 4.3 删除匹配模式的行



```shell
sed '/模式/d' file.txt
示例：删除包含 "error" 的行：

sed '/error/d' file.txt
```

## 5.插入和追加内容

### 5.1 在某行前插入内容



```shell
sed 'N i 新内容' file.txt
示例：在第 2 行前插入内容：

sed '2i 插入的内容' file.txt
```

### 5.2 在某行后追加内容



```shell
sed 'N a 新内容' file.txt
示例：在第 2 行后追加内容：

sed '2a 追加的内容' file.txt
```

## 6.替换指定行的内容

### 6.1 替换第 N 行的内容



```shell
sed 'Ns/.*/新内容/' file.txt
示例：替换第 2 行的内容：

sed '2s/.*/替换后的内容/' file.txt
```

### 6.2 替换指定范围的行内容



```shell
sed 'M,Ns/.*/新内容/' file.txt
示例：替换第 2 到第 4 行内容：

sed '2,4s/.*/新内容/' file.txt
```

## 7.显示匹配内容

### 7.1 仅显示匹配的行



```shell
sed -n '/模式/p' file.txt
示例：显示包含 "test" 的行：

sed -n '/test/p' file.txt
```

### 7.2 显示行号

`sed '=' file.txt`

## 8.结合正则表达式使用

### 8.1 替换以特定字符串开头的行

`sed 's/^旧字符串/新字符串/' file.txt`

### 8.2 替换以特定字符串结尾的行

`sed 's/旧字符串$/新字符串/' file.txt`

### 8.3 删除空行

`sed '/^$/d' file.txt`

## 9.批量处理文件

对多个文件进行替换：

`sed -i 's/旧字符串/新字符串/g' *.txt`

## 10.实际案例

### 10.1 删除所有以 # 开头的注释行

`sed '/^#/d' config.txt`

### 10.2 将第 2 到第 4 行替换为新内容

`sed '2,4c 替换后的新内容' file.txt`

### 10.3 替换文件路径中的斜杠

`sed 's|/|\\|g' file.txt`

### 10.4 为每行末尾添加分号

`sed 's/$/;/' file.txt`

### 10.5 为匹配的行添加标记

`sed '/模式/s/^/[标记] /' file.txt`
